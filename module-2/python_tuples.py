my_tuple = (1, 'set', 4.5, True)

print(my_tuple.count('set'))

# You can do count and index with both lists and tuples 
# the difference in lists and tuples is that tuples are immutable
# This means they cannot be modified